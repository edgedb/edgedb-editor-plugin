# AUTOGENERATED WITH `make regen-grammar`

---
match: |
  (?ix) \b(?<!::|\.|\.<|\.>)(
    (?# special case)
    (named \s+ only)
    |
    (as \s+ text)
    |

    (?# unreserved)
    abstract | after | alias | all | allow |
    annotation | as | asc | assignment | before |
    by | cardinality | cast | config | database |
    ddl | default | deferrable | deferred | delegated |
    desc | emit | explicit | final | first |
    from | implicit | index | infix | inheritable |
    into | isolation | last | link | migration |
    multi | object | of | oids | on |
    only | operator | overloaded | postfix | prefix |
    property | read | rename | repeatable | required |
    restrict | role | savepoint | scalar | schema |
    sdl | serializable | session | single | source |
    system | target | ternary | then | to |
    transaction | type | using | verbose | view |
    write
    |
    (?# reserved)
    alter | analyze | and | anyarray | anytuple |
    begin | case | check | commit | configure |
    create | deallocate | declare | delete | describe |
    detached | discard | distinct | do | drop |
    else | empty | end | execute | exists |
    explain | extending | fetch | filter | for |
    function | get | global | grant | group |
    if | ilike | import | in | insert |
    introspect | is | like | limit | listen |
    load | lock | match | module | move |
    not | notify | offset | optional | or |
    order | over | partition | policy | prepare |
    raise | refresh | reindex | release | reset |
    revoke | rollback | select | set | start |
    typeof | union | update | variadic | when |
    window | with
  )\b
...