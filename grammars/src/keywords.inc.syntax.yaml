# AUTOGENERATED WITH `make regen-grammar`

---
match: |
  (?ix) \b(?<!::|\.|\.<|\.>)(
    (?# special case)
    (named \s+ only)
    |
    (as \s+ text)
    |

    (?# unreserved)
    abort | abstract | access | after | alias |
    all | allow | annotation | applied | as |
    asc | assignment | before | cardinality | cast |
    config | conflict | cube | current | database |
    ddl | declare | default | deferrable | deferred |
    delegated | deny | desc | empty | except |
    extension | final | first | from | function |
    implicit | index | infix | inheritable | instance |
    into | isolation | last | link | migration |
    multi | object | of | only | onto |
    operator | optionality | order | orphan | overloaded |
    owned | package | policy | populate | postfix |
    prefix | property | proposed | pseudo | read |
    reject | release | rename | required | reset |
    restrict | role | roles | rollup | savepoint |
    scalar | schema | sdl | serializable | session |
    source | superuser | system | target | ternary |
    then | to | transaction | type | unless |
    using | verbose | version | view | write
    |
    (?# reserved)
    alter | analyze | and | anyarray | anytuple |
    begin | by | case | check | commit |
    configure | create | deallocate | delete | describe |
    detached | discard | distinct | do | drop |
    else | end | execute | exists | explain |
    extending | fetch | filter | for | get |
    global | grant | group | if | ilike |
    import | in | insert | introspect | is |
    like | limit | listen | load | lock |
    match | module | move | never | not |
    notify | offset | on | optional | or |
    over | partition | prepare | raise | refresh |
    reindex | revoke | rollback | select | set |
    single | start | typeof | union | update |
    variadic | when | window | with
  )\b
...